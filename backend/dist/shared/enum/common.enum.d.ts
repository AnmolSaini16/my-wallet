export declare enum ErrorMessage {
    BankNotFoundMsg = "Bank not found with specified account",
    InvalidTransaction = "No transaction found with specified account"
}
export declare enum BankBalanceUpdateType {
    Increment = "Increment",
    Decrement = "Decrement"
}
